
import { isActiveTab } from 'frontend/helpers/is-active-tab';
import { module, test } from 'qunit';

module('Unit | Helper | is active tab');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isActiveTab([42]);
  assert.ok(result);
});

