

<p>If bibpost with id <i>{{bibId}}</i> is marked ok for lending I will redirect to the cas-server (<i>{{application.config.casurl}}</i>)
</p>
<div class="row vertical-space-bottom-20">
	<div class="col-xs-12">
	{{pick-location locations=application.locations selectedLocation=application.selectedLocation label=(t 'start.labelForLocationDropdown')}}
	</div>
</div>

<div class="row">
	<div class="col-xs-12">
	{{pick-type-of-loan loantypes=application.loantypes selectedLoantype=application.selectedLoantype label=(t 'start.labelForLoantypeDropdown')}}
	</div>
</div>

{{outlet}}
